#include "Mesh.h"

std::unique_ptr<StaticMesh> Pillow::Graphics::CreateCube(float xHalf, float yHalf, float zHalf)
{
   return std::unique_ptr<StaticMesh>();
}

std::unique_ptr<StaticMesh> Pillow::Graphics::CreateSphere(float radius)
{
   return std::unique_ptr<StaticMesh>();
}
